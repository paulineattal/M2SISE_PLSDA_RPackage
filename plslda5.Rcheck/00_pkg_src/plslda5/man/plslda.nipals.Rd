% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nipals.R
\name{plslda.nipals}
\alias{plslda.nipals}
\title{plslda.nipals,
Nipals}
\usage{
plslda.nipals(X, y, ncomp, max.iter, tol)
}
\arguments{
\item{X}{entry matrix containing observations and variables}

\item{y}{the target variable}

\item{ncomp, }{number of components}

\item{max.iter, }{maximum number of iterations until convergence}

\item{tol, }{tol is the value that determines the convergence of the current weight and old weight, and which we put as parameter value of the nipals function which is called in this fit function}
}
\value{
It returns a plsda object as a list which contains :
\code{comp_X}
matrix of latent variables of X
\cr
\code{poid_X}
the weight matrix of the principal components of X
\cr
\code{comp_Y}
matrix of latent variables of Y
\cr
#' \code{poid_Y}
the weight matrix of the principal components of Y
\cr
#' \code{Y.iter}
matrix containing the scores of the target variable
\cr
#' \code{quality}
used for display in summary function
\cr
}
\description{
nipals is an internal function used in the fit function
}
\examples{
data(iris)
formula = Species~.
X <- as.matrix(model.matrix(formula=formule, data = iris)[,-1])
y <- as.factor(model.response(model.frame(formula=formule, data = iris)))
ydum <- plslda.dummies(y)

plslda.nipals(X=X, y=ydum, ncomp = 2, max.iter = 100, tol = 1e-06)

}
